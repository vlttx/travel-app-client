export default (state = [], action) => {
	switch (action.type) {
		default:
		  return state
	}
}

//since this is the default export we can choose to name
//it whatever we want when we import it to index.js
